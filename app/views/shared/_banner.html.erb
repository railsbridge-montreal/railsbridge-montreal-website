<div class="banner">
  <div class="container">
    <div class="jumbotron">
      <div class="row">
        <div class="col-md-6">
          <h1><%= t('.tagline') %></h1>
        </div>
        <div class="col-md-6">
          <p><%= raw t('.event_description') %></p>
          <p><%= raw t('.event_date') %></p>
          <p>
            <%= link_to new_registrant_path, class: 'btn btn-primary btn-lg' do %>
              <i class="fa fa-rocket"></i> <%= t('.registrant.register') %>
            <% end %>
          </p>
        </div>
      </div>
    </div>
  </div>
</div>
